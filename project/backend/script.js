const { Client } = require('pg'); const client = new Client({ connectionString: process.env.DATABASE_URL || 'postgresql://postgres:postgres@localhost:5432/investment_portal' }); client.connect().then(() => client.query('SELECT column_name FROM information_schema.columns WHERE table_name = ''old_investors''').then(res => { console.log(res.rows); client.end(); })).catch(err => console.error(err));
